# 세션 로그 — 2026-05-22

각 항목 형식:
- **지시** — 사용자가 한 말 (원문 인용)
- **수행** — 실제 한 작업 (명령/파일 변경/조회)
- **결과** — outcome, 발견, 산출물 (URL·ID·경로)

---

## Prelude (이번 turn 이전 요약)

이 turn 이전 작업은 별도 상세 로그 없이 진행됨. 핵심 결과만 정리:

### 1. Cloudflared 터널 재발급
- 사용자가 `https://annotation-tulsa-chamber-can.trycloudflare.com/` 접속 안 된다 보고
- 진단: WSL2 IPv6-only 라우팅 문제 + quick tunnel URL 자체는 유효했음. 하지만 사용자 측 어떤 환경에서 차단/캐시 가능성이 있어 cloudflared 재시작 요청
- 작업: PID 7021 kill → 새 cloudflared (`localhost:3101` 대상) 띄움, PID 12051
- 새 URL: `https://spin-releases-layout-theology.trycloudflare.com` (Phase 1 검증: HTTP/2 200 + Next.js)
- 카카오 챗봇 스킬서버 URL 도 같이 갈아야 함 확인 → `surgeons-shares-mutual-segments.trycloudflare.com/webhook/kakao` (이미 죽어 있던 옛날 URL) 대신 `https://spin-releases-layout-theology.trycloudflare.com/webhook/kakao` 권장
- 카카오 webhook POST 검증 통과 (welcome 메시지 반환)
- 비고: ngrok (phh `:3100`, `scribble-failing-ludicrous.ngrok-free.dev`) 은 그대로

### 2. 월드비전 광고 misclassification 조사
- DB(`scamguardian.sqlite3`) 의 오늘 run 조회 → `db807a16…` (04:41), `b7cec99c…` (04:32) 두 건 모두 "로맨스 스캠" 으로 분류된 사실 확인
- URL: `https://youtube.com/shorts/N32bfTUnnWw`
- 원인: Whisper STT 가 광고 BGM 위에서 "엄마가 도와줄게" 같은 짧은 구를 7~8회 반복 환각 → transcript 가 *정서적 친밀 표현* 만 남음 → mDeBERTa zero-shot 분류기가 "로맨스 스캠" anchor 로 매칭
- `total_score=0`, 플래그 0개 — Identity Boundary 측면 "사기다" 단정은 안 했고 *라벨* 만 틀림

### 3. Whisper 환각 차단 Plan + 구현 시도 + 전체 revert
- Plan 모드로 `whisper-validated-fairy.md` 설계 (3-layer: verbose_json segment 필터 + 반복 구 축약 + 신뢰도 신호)
- 사용자 승인 후 구현: `pipeline/stt.py` 환각 필터 추가 / `experiments/v4_whisper/chunker.py` 동일 패턴 미러 / `tests/test_stt_hallucination_filter.py` 11개 신규
- 모든 테스트 통과 (신규 11/11, 전체 321/321), 월드비전 URL 재실행으로 효과 확인 (transcript "엄마가 도와줄게" 7회 → 광고 진짜 내레이션이 드러남, Phase 1.5 게이트 `undetermined (0.00)` 으로 빠짐)
- 사용자가 즉시 revert 요청 → `git checkout -- pipeline/stt.py experiments/v4_whisper/chunker.py` + `rm tests/test_stt_hallucination_filter.py` 로 원상복구
- **현재 작업 디렉토리 상태**: stt/chunker 모두 세션 시작 시점 동일, 테스트 신규 파일 없음

---

## 본 로그 시작 (이번 turn 부터 자세히 기록)

### 2026-05-22 — turn N
- **지시**: "오늘 분류기랑 추출기쪽 건든게 있어? 앞으로 내가 하는거 따로 마크 다운 형식으로 뭘 했는지 자세하게 기록해줘"
- **수행**:
  1. `git log --since/until` 로 오늘 `pipeline/classifier.py`, `pipeline/extractor.py` 변경 이력 조회 — 커밋 0건
  2. `git status --short pipeline/` 로 작업 디렉토리 미커밋 변경 조회 — 0건
  3. `tasks/` 폴더 컨벤션 확인 (`todo.md`, `lessons.md` 있음)
  4. 이 파일(`tasks/session_2026-05-22.md`) 생성, Prelude 로 이번 turn 이전 흐름 요약
- **결과**:
  - 분류기/추출기는 오늘 *건드린 적 없음* (확정)
  - 활동 로그 파일 신규: `/home/mpwsl2/a-eye/idea_2/scamguardian-v2-kyy/tasks/session_2026-05-22.md`
  - 다음 turn 부터는 prelude 없이 항목 추가만 진행

### 2026-05-22 — turn N+1
- **지시**: "웹에서 분석 실행하는데 분명 'Whisper 환각 차단' 적용해서 그런지 스캠 유형 분석 결과가 원래는 정상이라고 나오던 영상이 사기 영상이라고 분류가 되는데 왜 이런 문제가 발생한거야?"
- **수행**:
  1. 코드 revert 확인 — `git status` 깨끗, `pipeline/stt.py:127-133` 의 `client.audio.transcriptions.create(...)` 호출이 원본 그대로 (`model/file/language` 만 넘김, verbose_json/temperature 없음)
  2. 5시 이후 DB run 조회 (`scamguardian.sqlite3`, `analysis_runs` 테이블, `created_at >= 2026-05-22T05:00`)
  3. 두 영상 발견:
     - `N32bfTUnnWw` (월드비전) — `fdf26b9a` 05:28, `scam_type="부동산 사기"`, transcript 깨끗 (환각 없음)
     - `uwXFkK9ciWs` (평창 다리 위 미담) — `baf4bd5f`/`3763814d`/`cedaf940`/`39271fd3` 4건 모두 `scam_type="코인 사기"`, transcript 깨끗
  4. gate decision + classifier confidence 조회 — 5건 모두 `bucket="undetermined"`, `source="fallback"`. 3건의 reason = **`"게이트 호출 실패: Error code: 529 ... Overloaded"`** (Anthropic Haiku API 과부하)
  5. classifier confidence: 0.15~0.23, `is_uncertain=1` (분류기 스스로 불확실 표시)
  6. `active_models.json` 조회 — fine-tuned classifier 는 *disabled* (DiaConfig 호환성 오류로 zero-shot fallback 매 요청 발생, 깨진 경로 제거됨)
  7. `uwXFkK9ciWs` 의 historical 분석: 04:48 첫 분석 = `scam_type=""` (공백, 라벨 안 붙음). 05:29~05:44 = "코인 사기" 4번 연속
- **결과**:
  - 사용자 가설 ("환각 차단 때문") **기각** — `uwXFkK9ciWs` 는 정상 음성이라 환각 차단 코드 있든 없든 *동일한 transcript*. 게다가 코드는 이미 revert 됨.
  - **진짜 원인 (DB 증거 기반)**:
    1. Anthropic Claude Haiku API 가 overloaded (Error 529) — Phase 1.5 게이트가 *fallback* 으로 빠짐. 원래 게이트가 정상 작동하면 비-사기 콘텐츠를 *판단 불가* 로 빠뜨려 분류 라벨을 표면에서 숨기지만, fallback 일 때 분류기 결과가 그대로 노출됨.
    2. mDeBERTa zero-shot 의 구조적 한계 — 후보 라벨 중 *가장 가까운 것* 을 무조건 picking. 사기와 무관한 입력도 어떤 사기 라벨에 강제 매핑.
  - 04:48 → 05:29 사이 어느 시점에 Anthropic overloaded 시작 → 그 이후 게이트 호출 모두 fallback → 분류 라벨 노출 시작
  - 권장: (1) Anthropic 안정화 대기, (2) uvicorn 재시작으로 stale 모듈 가능성 제거, (3) 장기 — 게이트 fallback 시 분류 라벨 카드 노출 안전 정책 검토 (Identity Boundary 보강)

### 2026-05-22 — turn N+2
- **지시**: "스크립트 폴더 내에 스타트 kyy라는 스크립트 파일 진행 시켜줘"
- **수행**:
  1. `bash scripts/start_kyy.sh` 실행 — 이전 cloudflared (PID 12052) kill, 이전 backend/frontend kill 후 새로 띄움
  2. 스크립트 출력 URL: `https://spin-releases-layout-theology.trycloudflare.com` (직전과 우연히 동일)
  3. 60초간 polling 했는데 backend(8001) 아직 안 떴고 public tunnel 530 지속
  4. backend 로그 보니 SBERT(`paraphrase-multilingual-MiniLM-L12-v2`) 모델 로딩 중 — uvicorn 정상 boot 진행
  5. cloudflared 로그에 *이전 인스턴스의 graceful shutdown 잔여 ERR* 다수 + 새 인스턴스의 `Registered tunnel connection` 06:03:21 보임 (두 프로세스가 같은 로그 파일에 동시 append 한 결과)
  6. 그 후 ss/curl 확인 시점 — backend 8001/frontend 3101 모두 LISTEN OK, localhost:3101 → 200, 그러나 public tunnel → 530 지속
  7. cloudflared metrics (`127.0.0.1:20241/metrics`) 확인 — `total_requests=0`, `request_errors=0` → cloudflared 가 *edge 트래픽 0개* 수신, edge ↔ cloudflared connection 유지 실패 진단
  8. cloudflared 만 kill (PID 46457) + 로그 truncate + 새로 spawn (PID 49112)
  9. 새 URL: `https://ownership-unsubscribe-give-rational.trycloudflare.com` (1초 안에 발급), public HTTP/2 200 확인
- **결과**:
  - **새 cloudflared URL: `https://ownership-unsubscribe-give-rational.trycloudflare.com`**
  - backend 8001 ✅ (uvicorn PID 46587), frontend 3101 ✅ (next PID 46521), cloudflared ✅ (PID 49112)
  - **⚠️ 카카오 챗봇 스킬서버 URL 갱신 필요**: `https://ownership-unsubscribe-give-rational.trycloudflare.com/webhook/kakao`
  - 발견: start_kyy.sh 의 cloudflared 첫 등록 후 edge connection 이 retain 안 되는 케이스 가끔 발생 → 처음 한 번은 cloudflared 만 추가 재시작 필요할 수 있음 (메커니즘 불명, quic receive buffer 경고 의심)

### 2026-05-22 — turn N+3
- **지시**: "새주소 발급해줘"
- **수행**:
  1. `pkill -f "cloudflared.*3101"` (이전 PID 49112 종료) — 첫 시도는 exit 144 (pkill 의 signal 종료 exit 코드) 였지만 정상 종료된 것
  2. `cloudflared-kyy.log` truncate + 새 cloudflared spawn (PID 50279) `--url http://localhost:3101`
  3. 1초 안에 새 URL 로그에 등장
  4. 5초 대기 후 public 200 OK + `/webhook/kakao` POST → 카카오 welcome 메시지 정상 반환 확인
- **결과**:
  - **새 cloudflared URL: `https://builders-widespread-clouds-lone.trycloudflare.com`**
  - **카카오 챗봇 스킬서버 URL 갱신 필요**: `https://builders-widespread-clouds-lone.trycloudflare.com/webhook/kakao`
  - 이번엔 첫 spawn 만에 edge connection 정상 유지 (직전 turn N+2 의 retain 실패 미발생)

### 2026-05-22 — turn N+4
- **지시**: "내가 지금 분석했거든 그걸로 로그한번 다시 봐줄래? 얼마나 어디가 오래걸렸는지"
- **수행**:
  1. `backend-kyy.log` 에서 가장 최근 `/api/analyze` 요청 한 건 추출 (awk 로 시작·종료 구간만)
  2. 노이즈 (`Loading weights`, `Batches`, `huggingface.co` httpx) 필터링, Phase 마커 + timestamp 라인만 남김
  3. Phase별 timestamp 차분으로 소요 시간 계산
- **결과**:
  - 분석 대상: `https://youtube.com/shorts/L0tpacPNWvo` — 수분 매치 광고 (스킨케어, 사기 무관)
  - transcript 길이: 147자, scam_type 분류 결과: 투자 사기 (confidence 49.9% — 낮음)
  - 검출 신호: 0개
  - **총 소요 49초 (15:15:42 → 15:16:31)**, phase 분포:
    - Phase 1 STT: ~8초 (yt-dlp + Whisper API 정상)
    - Phase 1.5 Gate: ~7초 (Anthropic 529 Overloaded × 3 retry → fallback)
    - Phase 2 mDeBERTa: ~3초
    - Phase 3 LLM 통합: **~30초** ⚠️ (Anthropic 529 × 3 retry → 실패, 가장 큰 병목)
    - Phase 4/5: 거의 0초 (Serper skip + 종합만)
  - **49초 중 ~35초가 Anthropic Haiku Overloaded 의 retry/timeout 낭비**
  - 비교: 직전 run (15:14:32, 25.3초) 은 게이트가 normal 정상 응답 (2.0s) → Phase 2 skip → 16초만에 완료. Anthropic 변동성이 latency 의 주 결정 요인
  - 권장: (1) Anthropic 안정화 대기, (2) 단기 — `pipeline/llm_assessor.py` 의 retry/backoff/timeout 옵션 조정해서 fast-fail, (3) 장기 — fallback 시 사용자 체감 latency 위해 빠른 응답 정책

### 2026-05-22 — turn N+5
- **지시**: "llm에서 시간이 많이 걸린 것 같은데 어디 단계에서 오래 걸린거야?" + 후속 "phase3이면 근데 그럼 우리 사기 분류 위험요소는 클로드가 안매기잖아 우선"
- **수행**:
  1. Phase 3 안 sub-step timeline 정리 — ThreadPoolExecutor 병렬 안 LLM/GLiNER/RAG 의 각각 끝난 시점
  2. 핵심 발견: Phase 3 LLM 통합 30초 중 **첫 Claude API 호출 한 번이 26초** 걸림 (나머지 4초는 retry 2번)
  3. Phase 1.5 게이트 (147자 prompt) vs Phase 3 LLM 통합 (3,790자 prompt) 비교 — 같은 Haiku 인데 첫 응답 latency 가 2초 vs 26초. **프롬프트가 클수록 overloaded 상황에서 latency penalty 큼**
  4. CLAUDE.md 의 역할 분담 재확인:
     - 사기 유형 분류 본체 = Phase 2 mDeBERTa (Claude 는 재판정 보조)
     - 엔티티 본체 = Phase 3 GLiNER (Claude 제안은 threshold 넘으면 병합)
     - 위험 신호 본체 = Phase 4 Serper + 룰 기반 (Claude 제안 플래그는 0.5 배율 축소 반영)
     - 점수 합산 본체 = Phase 5 scorer.py
- **결과**:
  - **Claude 는 분류·신호의 *본체* 가 아니라 *2차 의견*** — 사용자 확인 통과
  - 방금 분석 (`L0tpacPNWvo` 수분 매치 광고) 결과: `scam_type=투자 사기` 는 mDeBERTa 판단 (Claude 없이도 나옴), `signals=0` 은 Phase 4 룰 0건 (Claude 와 무관)
  - fast-fail 권장 — `anthropic.Anthropic(timeout=8.0, max_retries=1)` 박으면 Anthropic overloaded 시 30초 → ~10초로 단축, 분류 품질 영향 거의 없음
  - 사용자 적용 의사 대기 중

### 2026-05-22 — turn N+6
- **지시**: "시간 단축 시키고 싶은데 혹시 VirusTotal 대기 문제 때문인걸까?" → "LLM 통합 부분을 수정할까?"
- **수행**:
  1. 방금 분석 로그에서 `[Phase 0] YouTube URL — VirusTotal skip` 확인 — VT 는 이번 49초에 0초 차지 (신뢰 플랫폼). 진짜 병목은 Anthropic Haiku 임을 다시 명확히
  2. Anthropic client init 위치 grep — `pipeline/llm_assessor.py:38`, `pipeline/context_chat.py:37` 두 곳에서 동일 패턴
  3. AskUserQuestion 으로 적용 범위 + 값 결정 받음:
     - 적용 범위: **LLM 통합만** (`llm_assessor.py:38`)
     - 값: **timeout=8.0, max_retries=1**
  4. `pipeline/llm_assessor.py:38` 한 줄 수정 — `anthropic.Anthropic(api_key=api_key)` → `anthropic.Anthropic(api_key=api_key, timeout=8.0, max_retries=1)` + 변경 사유 주석
- **결과**:
  - 변경 파일: `pipeline/llm_assessor.py` (한 줄 + 3줄 주석)
  - Phase 1.5 게이트 (`context_chat.py:37`) 는 *사용자 결정에 따라 그대로* — overloaded 시 여전히 ~7초 걸림
  - uvicorn `--reload` 가 변경 자동 감지해서 리로드 (다음 `/api/analyze` 부터 적용)
  - 예상 효과: Anthropic overloaded 시 Phase 3 LLM 통합 ~30s → ~10s. 총 분석 49s → ~30s (게이트 ~7s 는 그대로)
  - 검증 대기: 다음 overloaded 상황의 분석에서 backend log 의 retry 횟수 + 응답 latency 측정

### 2026-05-22 — turn N+7
- **지시**: "내가 지금 분석했거든 그걸로 로그한번 다시 봐줄래? 얼마나 어디가 오래걸렸는지" (fast-fail 적용 후 검증 의도)
- **수행**:
  1. backend log 에서 최근 `/api/analyze` 요청 markers 추출 (sed -n 으로 라인 범위 지정)
  2. 최근 1건: 15:43:45~15:44:21, middleware 36.7초, 검출 26.4초
  3. Phase별 timestamp + Anthropic API 호출 추출
- **결과**:
  - 분석 대상: `youtube.com/shorts/eHUhWBH6hmo` — "충남보령 미용실..." 뉴스/사고 영상, 사기 무관
  - **fast-fail 효과 검증 *못 함*** — 게이트(Phase 1.5)가 `scam_news_edu` (사기 뉴스·교육, 0.92) 로 분류하면서 `llm=skip` 시그널을 줘서 **Phase 3 LLM 통합 호출 자체 안 일어남**
  - Phase 별 시간: STT 9s + 게이트 10s (Anthropic 529×2 retry → 3차 200 성공) + Phase 2/3/4 skip 또는 빠름 = 26.4s 검출 + 다운로드/큐 = 36.7s middleware
  - 게이트의 *3차 시도 200 OK* 가 인상적 — Anthropic overloaded 가 *완화* 되어가는 신호일 수 있음
  - **검증 추천 시나리오**: 진짜 사기 콘텐츠 (보이스피싱/투자권유/사기광고) 로 게이트가 `normal/suspicious_content` → Phase 3 LLM 통합 호출되는 케이스에서 fast-fail 작동 확인
  - 게이트 path (`context_chat.py:37`) 는 사용자 결정에 따라 fast-fail 미적용 — 이번 10초 게이트는 *우연히 3차 시도 성공* 이라 운이 좋았던 거고, fallback 가는 케이스에선 여전히 ~7초 걸림

### 2026-05-22 — turn N+8
- **지시**: "신뢰도 30% 미만이면 스캠유형에서 정상이라고 뜨게 해"
- **수행**:
  1. 현재 코드 상태 조사:
     - `pipeline/config.py:1000` — `CLASSIFICATION_THRESHOLD = 0.3` **이미 30%**
     - `pipeline/classifier.py:190,222` — confidence < THRESHOLD 이면 `is_uncertain=True` **이미 표시**
     - `apps/web/src/app/page.tsx:500` — scam_type 라벨 *그대로 노출* + 경고만 추가 (L631 "분류 신뢰도가 낮아서 결과가 부정확할 수 있습니다")
     - 즉 *분류기는 이미 uncertain 표시*, *UI 가 그 정보를 라벨 숨기는 데 안 쓰고 있었음*
  2. Identity Boundary 충돌 정중 짚음 — "정상" 은 *반대 방향의 단정* 이라 실제 사기인데 분류기 놓친 케이스에서 사용자가 "ScamGuardian 이 정상이라 했으니 안전" 으로 오해할 위험. 권장: "분류 불가" / "판단 보류"
  3. AskUserQuestion 으로 결정:
     - 라벨: 사용자 선택 **"정상"** (사용자 의사 존중, Boundary 충돌 알고도 결정)
     - 적용 범위: "git 메인이 아니고 내 프로젝트의 메인 (= `apps/web/src/app/page.tsx`)" → **웹 UI 메인 페이지만** (결과 토큰 페이지 / 카카오 챗봇 / API 응답 schema 미변경)
  4. `apps/web/src/app/page.tsx:498-505` 의 스캠 유형 카드 렌더링 한 곳 수정:
     - 기존: `{(report.scam_type ?? "").trim() || "미분류"}`
     - 변경: `report.is_uncertain || (report.classification_confidence ?? 0) < 0.3 ? "정상" : (report.scam_type ?? "").trim() || "미분류"`
- **결과**:
  - 변경 파일: `apps/web/src/app/page.tsx` (한 곳, ternary 한 줄)
  - 효과: 메인 페이지의 스캠 유형 카드가 *confidence < 30% 또는 is_uncertain* 이면 "정상" 으로 표시
  - 변경 안 한 곳: 결과 토큰 페이지(`result/[token]/page.tsx`), 카카오 챗봇(`kakao_formatter.py`), API 응답 schema(`runner.py`/DB) — 사용자 결정에 따라 보존
  - Next.js dev 가 자동 리로드. 다음 분석 결과 카드에서 확인 가능
  - ⚠️ Identity Boundary 우려는 사용자에게 명시적으로 짚었고, 그래도 "정상" 선택 — 향후 *실제 사기 콘텐츠가 분류기에서 confidence 낮게 나오는* edge case 가 발견되면 그때 재논의 권장
